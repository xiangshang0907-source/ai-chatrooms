# AI Chatrooms 快速启动指南

## 🎉 Phase 2 开发完成！

恭喜！AI Chatrooms 的 Phase 2 MVP 已经完全实现，包括：

- ✅ 完整的用户认证系统（注册/登录）
- ✅ 房间管理（创建/加入/列表）
- ✅ 实时聊天界面
- ✅ AI 流式响应
- ✅ PostgreSQL 数据持久化
- ✅ 现代化前端 UI

## 🚀 启动应用

### 1. 启动数据库服务
```bash
docker-compose up -d postgres redis
```

### 2. 启动后端服务
```bash
cd backend
python -m app.main
```
后端将运行在：http://127.0.0.1:8000

### 3. 启动前端服务
```bash
cd frontend
npm run dev
```
前端将运行在：http://localhost:5174（或其他可用端口）

## 📱 使用应用

1. **打开浏览器**访问前端地址（通常是 http://localhost:5174）

2. **注册新账户**
   - 点击"立即注册"
   - 填写用户名、邮箱和密码
   - 自动登录到房间列表

3. **创建房间**
   - 点击"创建房间"按钮
   - 填写房间名称和描述
   - 设置最大参与者数量

4. **开始聊天**
   - 点击房间卡片进入聊天界面
   - 输入消息并发送
   - AI 助手将实时流式回复

## 🔧 技术特性

### 后端功能
- JWT 用户认证和授权
- PostgreSQL 数据库持久化
- RESTful API 设计
- Server-Sent Events (SSE) 流式响应
- AI 服务集成（千问模型）
- 房间和消息管理

### 前端功能
- React + TypeScript + Vite
- 响应式现代化 UI 设计
- 实时流式消息显示
- 自动认证状态管理
- 房间列表和聊天界面切换

## 🎯 后续开发 (Phase 3)

Phase 2 完成后，接下来可以考虑：

- 多 AI 角色配置
- 回合编排器
- 用户插话与打断/重试功能
- WebSocket 实时通信
- 更多 LLM 提供商支持

## 🐛 故障排除

如果遇到问题：

1. **数据库连接问题**：检查 Docker Compose 服务状态
2. **后端启动失败**：确认 Python 环境和依赖安装
3. **前端访问失败**：检查端口是否被占用，Vite 会自动选择可用端口
4. **API 调用失败**：确认后端服务正在运行且端口正确

## 💡 开发提示

- 后端 API 文档：访问 http://127.0.0.1:8000/health 确认服务运行
- 前端代理配置自动转发 API 请求到后端
- 数据库使用 PostgreSQL，确保 Docker 服务正常运行
- AI 响应使用千问模型，需要有效的 API 密钥配置

---

🎉 **恭喜完成 Phase 2 开发！现在你拥有了一个完整可用的 AI 聊天室应用！**
